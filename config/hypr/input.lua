-- omartia-dots-remux: Input config
-- Edit this for your keyboard / trackpad.

hl.config({
  input = {
    kb_layout = "us",
    repeat_rate = 40,
    repeat_delay = 250,
    numlock_by_default = true,
  },
})
