diff --git c/modules/bar/popouts/Wrapper.qml i/modules/bar/popouts/Wrapper.qml
index c0772e61..e18f2a8d 100644
--- c/modules/bar/popouts/Wrapper.qml
+++ i/modules/bar/popouts/Wrapper.qml
@@ -97,7 +97,7 @@ Item {
     }
 
     Binding {
-        when: root.isDetached || (root.hasCurrent && root.currentName === "wirelesspassword")
+        when: root.isDetached || (root.hasCurrent && root.currentName === "wirelesspassword") || (nexus.item?.needsKeyboardFocus ?? false)
 
         target: QsWindow.window
         property: "WlrLayershell.keyboardFocus"
diff --git c/modules/nexus/Nexus.qml i/modules/nexus/Nexus.qml
index 7c0e7d1e..5de14545 100644
--- c/modules/nexus/Nexus.qml
+++ i/modules/nexus/Nexus.qml
@@ -16,6 +16,7 @@ Item {
 
         onClose: root.close()
     }
+    readonly property bool needsKeyboardFocus: nState.subPageIdxStack.includes(7)
     property color blobColour: Colours.tPalette.m3surfaceContainerLow
 
     signal close
diff --git c/modules/nexus/NexusState.qml i/modules/nexus/NexusState.qml
index 8e8744b0..7198b2f3 100644
--- c/modules/nexus/NexusState.qml
+++ i/modules/nexus/NexusState.qml
@@ -17,6 +17,7 @@ QtObject {
     property string selectedNetworkSsid
     property string selectedEthernetInterface
     property bool networkDetailsFromSaved
+    property var pendingPasswordNetwork
 
     signal close
     signal subPageOpened(idx: int)
diff --git c/modules/nexus/PageCompRegistry.qml i/modules/nexus/PageCompRegistry.qml
index d7d59245..4184f7e2 100644
--- c/modules/nexus/PageCompRegistry.qml
+++ i/modules/nexus/PageCompRegistry.qml
@@ -64,6 +64,9 @@ QtObject {
                 Component {
                     SavedNetworksPage {}
                 }
+                Component {
+                    NetworkPasswordPage {}
+                }
             }
         },
         Component {
diff --git c/modules/nexus/common/NetworkList.qml i/modules/nexus/common/NetworkList.qml
index ddb6983c..923a2fc7 100644
--- c/modules/nexus/common/NetworkList.qml
+++ i/modules/nexus/common/NetworkList.qml
@@ -61,7 +61,10 @@ ItemList {
 
         onClicked: {
             if (!modelData.active) {
-                NetworkConnection.handleConnect(modelData);
+                NetworkConnection.handleConnect(modelData, null, network => {
+                    root.nState.pendingPasswordNetwork = network;
+                    root.nState.openSubPage(7);
+                });
                 currentSelected = true;
                 root.networkSelected(modelData);
             } else {
diff --git c/modules/nexus/pages/network/NetworkDetailPage.qml i/modules/nexus/pages/network/NetworkDetailPage.qml
index b320fa88..85088d3a 100644
--- c/modules/nexus/pages/network/NetworkDetailPage.qml
+++ i/modules/nexus/pages/network/NetworkDetailPage.qml
@@ -7,6 +7,7 @@ import Caelestia.Config
 import qs.components
 import qs.components.controls
 import qs.services
+import qs.utils
 import qs.modules.nexus.common
 
 // Detail / settings sub-page for the active Wi-Fi network. Reached by tapping
@@ -146,6 +147,49 @@ PageBase {
                 }
             }
 
+            ButtonBase {
+                id: connectBtn
+
+                visible: !root.isActive
+                fillWidth: true
+                shapeMorph: true
+                isRound: true
+                inactiveColour: Colours.palette.m3primaryContainer
+                inactiveOnColour: Colours.palette.m3onPrimaryContainer
+
+                implicitWidth: connectLayout.implicitWidth + Tokens.padding.extraLarge * 2
+                implicitHeight: connectLayout.implicitHeight + Tokens.padding.medium * 2
+
+                onClicked: {
+                    const network = root.ap || { ssid: root.ssid, isSecure: true, bssid: "" };
+                    NetworkConnection.handleConnect(network, null, net => {
+                        root.nState.pendingPasswordNetwork = net;
+                        root.nState.closeSubPage();
+                        root.nState.openSubPage(7);
+                    });
+                }
+
+                ColumnLayout {
+                    id: connectLayout
+
+                    anchors.centerIn: parent
+                    spacing: 0
+
+                    MaterialIcon {
+                        Layout.alignment: Qt.AlignHCenter
+                        text: "link"
+                        color: connectBtn.onColour
+                        fontStyle: Tokens.font.icon.medium
+                    }
+
+                    StyledText {
+                        Layout.alignment: Qt.AlignHCenter
+                        text: qsTr("Connect")
+                        color: connectBtn.onColour
+                    }
+                }
+            }
+
             ButtonBase {
                 id: disconnectBtn
 
diff --git c/modules/nexus/pages/network/NetworkPasswordPage.qml i/modules/nexus/pages/network/NetworkPasswordPage.qml
new file mode 100644
index 00000000..2e20c33c
--- /dev/null
+++ i/modules/nexus/pages/network/NetworkPasswordPage.qml
@@ -0,0 +1,149 @@
+pragma ComponentBehavior: Bound
+
+import QtQuick
+import QtQuick.Layouts
+import Caelestia.Config
+import qs.components
+import qs.components.controls
+import qs.services
+import qs.utils
+import qs.modules.nexus.common
+
+// Sub-page for entering a Wi-Fi password when connecting to a secured network
+// that has no saved profile. Opened from NetworkList via nState.openSubPage.
+PageBase {
+    id: root
+
+    readonly property var network: nState.pendingPasswordNetwork
+    property bool connecting: false
+    property bool failed: false
+    property bool connected: false
+
+    function submit(): void {
+        if (!root.network || passwordField.text.length < 8) {
+            passwordField.isError = true;
+            passwordField.forceActiveFocus();
+            return;
+        }
+
+        root.failed = false;
+        root.connecting = true;
+
+        NetworkConnection.connectWithPassword(root.network, passwordField.text, result => {
+            root.connecting = false;
+            if (result && result.success) {
+                root.connected = true;
+                root.nState.closeSubPage();
+            } else {
+                root.failed = true;
+                passwordField.isError = true;
+                if (root.network && root.network.ssid)
+                    Nmcli.forgetNetwork(root.network.ssid);
+            }
+        });
+    }
+
+    title: qsTr("Enter password")
+    isSubPage: true
+
+    ColumnLayout {
+        anchors.horizontalCenter: parent.horizontalCenter
+        anchors.top: parent.top
+        width: root.cappedWidth
+        spacing: Tokens.spacing.large
+
+        Connections {
+            target: root.nState
+        }
+
+        StyledText {
+            Layout.fillWidth: true
+            text: root.network ? qsTr("Enter the password for \"%1\"").arg(root.network.ssid) : ""
+            color: Colours.palette.m3onSurfaceVariant
+            font: Tokens.font.body.medium
+            wrapMode: Text.WordWrap
+        }
+
+        StyledTextField {
+            id: passwordField
+
+            Layout.fillWidth: true
+            enabled: !root.connecting
+            placeholderText: qsTr("Password")
+            leadingIcon: "key"
+            echoMode: TextInput.Password
+            supportingText: qsTr("WPA passwords are at least 8 characters")
+            errorText: root.failed ? qsTr("Connection failed — check the password") : qsTr("Password must be at least 8 characters")
+
+            onAccepted: root.submit()
+        }
+
+        RowLayout {
+            Layout.alignment: Qt.AlignRight
+            Layout.topMargin: Tokens.spacing.extraSmall - parent.spacing
+            spacing: Tokens.spacing.small
+
+            TextButton {
+                Layout.fillHeight: true
+                isRound: true
+                horizontalPadding: Tokens.padding.extraLarge
+                type: TextButton.Tonal
+                text: qsTr("Cancel")
+                onClicked: root.nState.closeSubPage()
+            }
+
+            ButtonBase {
+                id: connectBtn
+
+                shapeMorph: true
+                isRound: true
+                inactiveColour: Colours.palette.m3primary
+                inactiveOnColour: Colours.palette.m3onPrimary
+                stateLayer.disabled: root.connecting || passwordField.text.length < 8
+
+                implicitWidth: connectMetrics.width + Tokens.padding.extraLarge * 2
+                implicitHeight: connectMetrics.height + Tokens.padding.medium * 2
+
+                onClicked: {
+                    if (!root.connecting && passwordField.text.length >= 8)
+                        root.submit();
+                }
+
+                TextMetrics {
+                    id: connectMetrics
+
+                    text: qsTr("Connect")
+                    font: connectBtn.font
+                }
+
+                AnimLoader {
+                    id: connectContent
+
+                    anchors.centerIn: parent
+                    sourceComp: root.connecting ? connectLoadingComp : connectTextComp
+                    outAnimType: Anim.SlowEffects
+                    inAnimType: Anim.SlowEffects
+                }
+
+                Component {
+                    id: connectLoadingComp
+
+                    LoadingIndicator {
+                        implicitSize: Math.round(Tokens.font.body.medium.pointSize * 1.4)
+                        color: connectBtn.onColour
+                    }
+                }
+
+                Component {
+                    id: connectTextComp
+
+                    StyledText {
+                        text: connectMetrics.text
+                        font: connectBtn.font
+                        color: connectBtn.onColour
+                    }
+                }
+            }
+        }
+    }
+}
diff --git c/services/Nmcli.qml i/services/Nmcli.qml
index a480887f..7b40d7e5 100644
--- c/services/Nmcli.qml
+++ i/services/Nmcli.qml
@@ -74,7 +74,11 @@ Singleton {
             return false;
         }
 
-        return (error.includes("Secrets were required") || error.includes("Secrets were required, but not provided") || error.includes("No secrets provided") || error.includes("802-11-wireless-security.psk") || error.includes("password for") || (error.includes("password") && !error.includes("Connection activated") && !error.includes("successfully")) || (error.includes("Secrets") && !error.includes("Connection activated") && !error.includes("successfully")) || (error.includes("802.11") && !error.includes("Connection activated") && !error.includes("successfully"))) && !error.includes("Connection activated") && !error.includes("successfully");
+        if (error.includes("Connection activated") || error.includes("successfully")) {
+            return false;
+        }
+
+        return error.includes("Secrets were required") || error.includes("No secrets provided") || error.includes("802-11-wireless-security.psk") || error.includes("password for");
     }
 
     function parseNetworkOutput(output: string): list<var> {
